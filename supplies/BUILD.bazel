# Temporarily simplified until rules_nodejs and npm are properly configured
# load("@rules_nodejs//nodejs:defs.bzl", "nodejs_binary")
# load("@aspect_rules_ts//ts:defs.bzl", "ts_project")
# load("@aspect_rules_js//npm:defs.bzl", "npm_package")
# load("@aspect_bazel_lib//lib:tar.bzl", "tar")
# load("@rules_oci//oci:defs.bzl", "oci_image", "oci_push")

package(default_visibility = ["//visibility:public"])

# Placeholder target - supplies service temporarily disabled
# Will be re-enabled once rules_nodejs and npm are properly configured
filegroup(
    name = "artifact",
    srcs = [],
    visibility = ["//visibility:public"],
)

# Placeholder uber target
filegroup(
    name = "uber",
    srcs = [],
    visibility = ["//visibility:public"],
)
